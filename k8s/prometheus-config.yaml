apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: dotnetapp
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: 'dotnet-app'
        static_configs:
          - targets: ['dotnetapp-service:80']  
